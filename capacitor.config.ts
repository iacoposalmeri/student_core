import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'student-core',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
